apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: prowlarr
  namespace: media
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: prowlarr
  endpoints:
    - port: metrics
      path: /metrics
      interval: 4m
      scrapeTimeout: 90s
