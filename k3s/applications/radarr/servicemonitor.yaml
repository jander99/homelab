apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: radarr
  namespace: media
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: radarr
  endpoints:
    - port: metrics
      path: /metrics
      interval: 4m
      scrapeTimeout: 90s
