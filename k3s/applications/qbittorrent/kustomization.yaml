apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - servicemonitor.yaml
  # PREREQUISITE: Create and SOPS-encrypt these secrets before applying:
  # cp nordvpn-credentials.sops.yaml.example nordvpn-credentials.sops.yaml && sops --encrypt --in-place nordvpn-credentials.sops.yaml
  # cp qbittorrent-credentials.sops.yaml.example qbittorrent-credentials.sops.yaml && sops --encrypt --in-place qbittorrent-credentials.sops.yaml
  # Then uncomment the lines below:
  # - nordvpn-credentials.sops.yaml
  # - qbittorrent-credentials.sops.yaml
