---
# k3s/infrastructure/configs/kopiur-policies/snapshotpolicy-qbittorrent-gluetun.yaml
apiVersion: kopiur.home-operations.com/v1alpha1
kind: SnapshotPolicy
metadata:
  name: qbittorrent-gluetun
  namespace: qbittorrent
spec:
  repository:
    kind: ClusterRepository
    name: nas
  sources:
    - pvc:
        name: gluetun-config
      sourcePathOverride: /gluetun
  copyMethod: Snapshot
  volumeSnapshotClassName: longhorn-snapclass
  compression:
    compressor: zstd
  retention:
    keepHourly: 24
    keepDaily: 14
    keepWeekly: 8
    keepMonthly: 6
  verification:
    quick:
      schedule:
        cron: "H 3 * * *"
  credentialProjection:
    enabled: true
  mover:
    # LinuxServer qbittorrent image (PUID=1027, PGID=100); same recipe as
    # prowlarr/radarr/sonarr. Pinned to the gluetun-config PVC so the
    # VPN credentials and forward-port history survive pod restarts.
    securityContext:
      runAsUser: 1027
      runAsGroup: 100
    podSecurityContext:
      fsGroup: 100
      fsGroupChangePolicy: OnRootMismatch
    cache:
      capacity: 1Gi
      storageClassName: local-path
      mode: Ephemeral
